# Milestones completed

The record of what has shipped, milestone by milestone. `ROADMAP.md` is forward-looking only;
when a milestone there is done, its row moves here with the date and release it landed in.
Append-only, newest last.

| # | Milestone | Done meant | Landed |
| --- | --- | --- | --- |
| M0 | Foundations | Repo public, tooling live, schema validated on paper against the five queries, ADRs 0001–0009 accepted | 2026-08-25 |
| M1 | Docket registry | Dockets table ingested (metadata only, no PDFs); filter application positively asserted; the registry validates extracted citations | 2026-08-25 — full registry walked on RMI-AI-MACHINE (32,604 dockets), now the production store |
| M2 | Filings + decisions ingest | Forward capture of the filings and decisions tables into the event ledger; documents hashed and stored; errata detection | 2026-08-26 in production (errata re-check scheduled 2026-08-27, v2026.08.35: a slice of the held files every pass, oldest-checked first) |
| M3 | Docket sheets + permanent URLs | One chronological page per proceeding at a stable, guessable URL (ADR 0013) | 2026-08-26, v2026.08.2 — live at [docketyard.org](https://docketyard.org) on one Lightsail instance (ADR 0012), polling forward every 30 min, Litestream + blob sync to S3 |
| M4 | Alerting | Docket subscriptions, email delivery, silent-failure detection per `docs/alerts.md` | 2026-08-26, v2026.08.4–11 — confirmed-opt-in subscriptions (ADR 0011) with addresses ciphertext at rest (ADR 0014); per-pass and daily alerts over SES with bounce/complaint feedback; hourly off-box heartbeat on captures, events, documents and delivery. First real alert delivered 2026-08-26 |
| M5 | Launch | About, coverage, methodology, corrections and privacy pages published; **the wedge is live** | 2026-08-26, v2026.08.9–11 — coverage measured from the store; corrections are of this record against the Board's, seven days usual and not guaranteed; `hello@docketyard.org` routed. Unannounced |
| M6 | Party module (ADR 0004) | "Filed for" strings resolved to entities with aliases and successions, provenance on every link; a Parties view on every sheet; subscribe by party | 2026-08-26, v2026.08.14–15 — 1,097 parties and 3,989 links resolved in production; `/parties` is a facet, not an address (ADR 0013 addendum); service-list predicate deferred to extraction |
| M7 | Statistics page | What the record holds and what moves, every number measured (`docs/stats.md`); nothing about readers | 2026-08-26, v2026.08.17–19 — `/stats`, column charts drawn server-side from the same rows |
| M9 | Bulk data and JSON (rest of F5) | A nightly scrubbed snapshot with measured manifest, schema and licence at `/data`; JSON twins of every docket, sub-docket, filing and decision address | 2026-08-26, v2026.08.21–22 (PR #2) — CC0 for the raw index; the enriched party layer withheld pending the licence review in `docs/licensing.md`; the scrub is an allowlist that fails rather than publishes, and caught Litestream's bookkeeping tables on its first live run |
| M8 | Feeds and webhooks (rest of C1) | Atom feeds per docket, per party and agency-wide from the same events as the email alerts; signed webhooks carrying the same payload, confirmed like an address (migration 0008) | 2026-08-26, v2026.08.20 (PR #1) — reviewed by code, security and schema passes before merge; first ping delivered, signature verified, and confirmed against a throwaway endpoint the same day |
| M10 | Party pages (ADR 0015) | A permanent address per party at `/p/<id>` — names with provenance, joins, successions, dockets with captions; 301 from every folded id, slug and the old feed path; `docketyard parties join`/`unjoin` | 2026-08-26, v2026.08.24 (PR #3) — reviewed by code, security and schema passes; migration 0009 forbids deleting or renumbering a party; verified from outside on 10,110 parties (UP: 3,441 filings in 318 dockets, 54 ms). Shipped through a GitHub Actions outage: the PR's CI run was orphaned, CI on `main` confirmed the merge |
| M11 | Document viewer (ADR 0013 addendum) | A record's file shown beside its docket and entry at `/filing/{id}/view`, `/decision/{id}/view` — parties, every file, neighbours on the sheet, cite line, follow form; the bytes at a permanent address `/document/{sha256}.{ext}`, inline for a PDF or image, fetched from S3 when the instance has pruned them and served only if the hash matches | 2026-08-27, v2026.08.31 (PR #10) — reviewed by code (8 finders), security and ingest passes; the reviews found the production framing headers would have blanked the frame, a descriptor leak and non-PDF kinds under a `.pdf` name, all answered before merge; a document host's refusal is now captured with its status and rested a week |
| — | Environmental comments, F1's third record row (v2026.08.42) | The Board publishes three tables per proceeding and the record held two: found on AB 55 (Sub-No. 794X), 70 records here against 6 unheld comments. A typed record with the commenter's own words, submitter, organisation, location and attachment, on the sheet beside filings and decisions, at its own permanent address `/comment/<number>`, searchable by those words, watched forward and walkable back. The key is the comment NUMBER, not the endpoint's row id as filings and decisions use, because no cell of a comment row prints that id — so the corroboration check that stops a column reorder mis-filing a record could not be written against it; the number is printed AND carried in its own cell's anchor, and over 2,385 comments sampled across four years it never repeated. No name is masked: a masking design was drafted and dropped because the same name sits in the comment's own words (5 of 76 measured), in the attachment, and in the extracted text OCR will produce — the reasoning is in migration 0011's header so it is not re-proposed blind. Measured, not assumed: a THIRD date-pair spelling (`startDate`/`endDate`); no `sort_by` accepted at all, eight candidates each answering the empty envelope with a 200; one comment spanning one row per attachment; half the rows printing `--` for the words. The quiet-week proof demonstrates an empty week with a wider request rather than trusting an envelope that cannot tell a quiet table from a broken one, and `last_event` is scoped to filing/decision types so a third writer cannot mask a filings outage  Shipped v2026.08.42; the archive wave then walked 2000-09-22 → 2026-08-26 (34,255 comments, 282 months done, 18 proven empty, none capped) and disproved the address: two numbers name two DIFFERENT comments each, so v2026.08.43 moved the canonical address to `/d/<docket>/comment/<number>`, the store's own key, with the bare number kept as a short form that redirects or, for those two, names both | 2026-08-31 |
| — | A read-only machine-agent surface (F7, v2026.08.44, PR #12) | Assistants already answer STB questions, and they answer them from training data — inventing docket numbers and service dates that look exactly like real ones. `POST /mcp` (Streamable HTTP, MCP 2025-11-25, stateless, no SSE) offers four tools over the reads the pages already do: search the record, read a docket sheet, read an environmental comment, and ask what the record does not hold. Two constraints travel with it rather than being left to good intentions: read-only, enforced by the module's imports, the web tier's `query_only` connection and a test that refuses a write-path import; and every answer carries its caveats IN the string, because an assistant is handed a string and will quote it — a miss reads as "an absence in this record, not proof of absence at the Board", which is the one failure this surface exists to prevent. `robots.txt` now names thirteen AI agents and welcomes them, training included, because the raw index is dedicated to the public domain and cannot be un-dedicated — with the party module carved out, the same carve-out the snapshot makes, pending the licence review. `docs/machine-surface.md` | 2026-08-31 |
| M12 | Migration A (ADRs 0021–0023) | The record's own text: `document_text` at one row per reading of one page, `document_pagination` as the coverage denominator, `ocr_run`, `text_payload`, the page index; the passes that fill them (`text paginate`, `text load`), the split search signature, and `/filing/<id>/text` — one address per record, every read page labelled, the band's operand or its absence; `/view` retired into the record page | 2026-09-03, v2026.09.2 (PRs #20–22, schema 17→19) — the schema by schema-critic and a four-lens panel, the code by eight-angle reviews with every finding verified against the store, PR #21 by the cloud ultra review; deployed behind the maintenance flag, integrity ok, and the tables stay empty until the instance is resized for the passes |

## Background work completed

| Work | Landed |
| --- | --- |
| Backfill wave 1 (2024-08 → watch start): 25 months, 4,864 documents; October 2025 declared empty (federal shutdown) | 2026-08-26 |
| Blob storage: S3 is the store, the instance a cache (prune after each 30-minute sync) — what lets wave 3's 150–250 GB pass through a 58 GB disk | 2026-08-26, v2026.08.16 |
| Extraction benchmark step 0: text layer for wave 1's files on RMI-AI-MACHINE — 4,273 PDFs, 2 image-only, 0 failed | 2026-08-26 |
| Streamed document downloads (v2026.08.25, PR #4): the record holds a 1.07 GB filing (FD 36500's merger application) that had the wave's documents step OOM-killed on the 2 GB instance; documents now stream to disk in 1 MB chunks and are hashed by chunks; the host's blob sync and prune leave the staging area alone | 2026-08-26 |
| `/contribute` (v2026.08.26, PR #5): ideas through an issue template, code through the repository under the CLA (pull requests held until its legal review), what helping does not get anyone; silent on money by the operator's decision | 2026-08-26 |
| Dockets sitemap hotfix (v2026.08.27, PR #7): the family fold was an unindexed self-join that hung on the full registry — now one grouped pass, 0.1 s; uvicorn's own access log (query strings to stdout) switched off | 2026-08-26 |
| Unified search (v2026.08.28, PR #6; `docs/search.md`): one box; a docket number opens its sheet; FTS5 over captions, party names and decision summaries (migration 0010, schema-critic reviewed), rebuilt by ingest when the record changes; `/search` without script, `/suggest` as-you-type; result pages never cached or indexed; Caddy's log keeps no query, Referer or Location | 2026-08-26 |
| Traffic counts (v2026.08.29, PR #8; `docs/traffic.md`): hourly counts by kind of page, response class and crawler-or-not, with size rounded to 64 KB and a latency bucket — counted in memory, written to `traffic.sqlite` beside the store, never into it; 90 days by the hour then by the day; the sentence the operator signed is on `/privacy`; `docketyard traffic` is the only view | 2026-08-26 |
| Backfill waves 2–3, documents (2026-08-26 → 27): every attachment the record lists for 1996 → 2024-07 fetched through the streaming fetcher — 77,565 documents held (32 GB on the instance, the store in S3) bar one legacy `/MPD/` URL that answers 403; 41 table months still `partial` and queued for a re-walk | 2026-08-27 |
| Backfill closed (v2026.08.30, PR #9): the 41 partial months of 1996–2000 proven empty by the walker's own neighbour-window reconciliation (`stb-data-source.md` § Measured 2026-08-27) and re-walked to `empty` at 13:58 UTC; `/coverage` lists no partial month. The whole record, 1996 → today, is held: 32,606 dockets, 77,565 documents | 2026-08-27 |
| `/api` and `/llms.txt` (v2026.08.32): F5's last edge — the human account of the machine surface (what answers, an example, the licence, what is stable, what is asked of a client) and an llms.txt rendered from the same source as the trust pages with the coverage numbers; the OpenAPI description points at `/api`. Viewer follow-ups in the same release: one store fetch per hash, `Retry-After` on the transient 503, a bucket without keys refuses to start | 2026-08-27 |
| `docketyard gap open/close` (v2026.08.34): the operator's record of an outage, written to `coverage_gap` for `/coverage` and cited by the late alert entries its catch-up carried — the window reaches past the gap's end by the lateness threshold, in the alert builder too | 2026-08-27 |
| Errata re-check scheduled (v2026.08.35): every pass re-fetches 40 held files (≤64 MB, none within 30 days of its last check), the longest-unchecked first, after the alerts — the whole record about every six weeks; the capture ledger is the last-checked column, so no schema change; reviewed by the ingest specialist, which also found and fixed a chain break when a new record cites an already-held URL | 2026-08-27 |
| Three held-metadata slices (v2026.08.36, chosen by delegation 2026-08-27): the citation resolver (F2's second half — `/d?q=` as a link service and `/cite` as JSON over every printed docket form, a decision by docket and service date, the Board's record ids; never a guess), the court-action register `/court` (D4's first slice: 341 notices, 491 entries, in 290 dockets) and the protective-order register `/protective` (D7's first slice: 683 motions, 695 entries, in 645 dockets, parties resolved, marked on the sheet); `docs/registers.md` | 2026-08-27 |
| Weekly traffic digest (v2026.08.37): the poller emails the operator the traffic table once a week (Monday 06:00 UTC, retried until SES accepts); nothing published, the signed privacy sentence unchanged | 2026-08-27 |
| Docket-type explainers (P2, v2026.08.39): the operator reviewed `docs/explainers.md` 2026-08-28; published as `/about/prefixes` and `/about/FD`, `/about/AB`, `/about/NOR`, `/about/EP`, `/about/MCF`, every statement graded by source, every figure measured on request, unconfirmed rows published marked as such; linked from every sheet and from `/stats` | 2026-08-28 |
| The proceeding, not its parent (v2026.08.40): the week names the sub-docket a filing was entered in — for AB, where a Sub-No. is each line a carrier abandons, the parent named a 391-proceeding series and named it badly (11.5% of AB roots carry a caption against 100% of the proceedings that file); a series docket now leads with an index of its proceedings, listing those the record holds nothing for because the number is the answer for a pre-1996 abandonment (AB 167: 995 listed, 952 empty), and AB 290 fell 3.3 MB → 164 KB; a `decided` date resolves to the sheet, never to a decision, because the record holds service dates alone and the two differ in 34 of 52 measured; the public snapshot exempts only tables SQLite itself types `shadow` | 2026-08-30 |
| A caption within the hour, and pages that read as lists (v2026.08.41): a proceeding reaches the filings table before the dockets table, so its docket row was minted from the filing and carried no caption — three were reading "(caption not yet observed)" on the home page. The forward pass now asks the Board's dockets table about each one BY NAME (`docketNum_three` is the sub-sequence, measured 2026-08-31: asking `AB 290` by family answers 392 rows, asking `AB 290 (Sub-No. 423)` answers one), bounded to one ask per docket per six hours, ten a pass, and eight attempts before it is reported and dropped — the capture ledger is the memory, so no schema change. Also: the court-action and protective-order registers stop reusing the sheet's 56px display number and gain a rule between groups; a document opens in the viewer beside its record, with the Board's own file beside it as its own icon | 2026-08-31 |
| Viewer hotfix (v2026.08.44): a viewer page whose neighbour on the sheet was an environmental comment answered 500 — `viewer.html` asked `record_path` for whatever kind the neighbour was, and a comment's address needs its docket. Reported by the operator against `/filing/240630/view` in AB 290 (Sub-No. 324X); 4 of 17 sampled viewer pages on comment-bearing sheets were failing. One `entry_path` now answers for all three kinds, and the four callers that had each spelled the branch by hand ask it | 2026-08-31 |
| Navigation Tiers 1–2 (v2026.08.45, PR #13; `docs/navigation-review.md`): the pages stop claiming what the ledger cannot support, and the search results print what the index already held. `covered()` matched the walk ledger's unsuffixed key exactly while `walk.py` appends `:{lo}..{hi}` for a partly-walked month, so the one month that had ever happened to read as never walked — hiding 91 filings and 16 decisions and, because it sat between the reader and everything older, walling off the whole 1996–2026 archive behind three weeks. The slice-key grammar now lives beside its writer and both store modules ask instead of parsing; the "does not cover this week" sentence renders only when the window is empty; a week outside [1990, today+366d] is a 404, ending an unbounded backward corridor and two date-arithmetic 500s; ~1,590 archive pages stop being headed "this week". `/coverage` had published 56 comment-walk months as gaps in filings and decisions, and now reports by table (operator's wording). Search rows lead with the row's own caption — 86,069 of 96,225 rows had an identifier-only title while 92.4% of docket rows carried the Board's caption one field away — with a highlighted FTS5 snippet saying why a row matched, marked with control characters and escaped before the tags are substituted (migration 0013, `INDEX_FORMAT` 3). Measured at the deploy: a full rebuild is 24.1 s at 96,225 rows, of which 5.6 s holds the write lock, which withdrew a claim three review passes had believed | 2026-09-01 |
| An AB sub-docket follows itself (v2026.08.46, PR #14; `navigation-review.md` A6): `/d/AB-55/sub/794X` offered "Follow this docket" and subscribed the address to all 766 proceedings under AB 55, which the reader learned from a confirmation email naming AB 55. Whether the fold is right depends on what a Sub-No. means and the Board does not use it for one thing — for FD it is a phase of one proceeding, for AB it IS the proceeding (each line a carrier abandons), and AB is where the lay reader is (52.1% of AB captions name a county against 6.1% of FD). AB stops folding, every other prefix keeps folding, the rule has one home in `subscriptions.follow_target`, and existing subscriptions are untouched. The alert builder needed no change — it already matched a subscription's docket or its children. `/d/<docket>/sub/<n>/feed` now exists, 301'ing to the family's feed under a folding prefix where it used to 404, and the page names what a follow covers before the email does. Review found four: a suffix on a parent is not a sub-docket, the feed guard checked the family, it built a 995-sub-docket sheet to do it, and an address holding both a docket and a sub-docket under it was mailed one filing twice | 2026-09-01 |
| A way into the archive (v2026.08.47, PR #15; `navigation-review.md` § C, Tier 3's first two): Tier 1 had made ~1,590 archive week pages correct and reopened thirty years, and the only way to reach 2010 was clicking "previous week" sixteen hundred times — the weeks being in neither the sitemap nor `llms.txt`, so no search engine could stand in for the index either. `/weeks` is one aggregate over the two record tables (35 ms for 1,550 weeks across 31 years, against the review's ~120 ms estimate), grouped by year, memoised per store stamp, in the sitemap and in `llms.txt`; the home page's "past weeks" pointed at a single week and now points here, and the footer carries it onto the ~107,000 record addresses whose only links were a docket and a PDF. And the way up: `docket_sheet` is asked for the page's own id and a sub-docket has no children, so a child's family query returned only itself — 952 of AB 167's 995 proceedings were cul-de-sacs, and every sub-docket sheet now names its series and links to it. Review found six, including a `COUNT(*)` over the unindexed `parent_docket_id` that scanned 32,605 dockets on every sub-docket page | 2026-09-01 |
| `/parties` is a page before it is a search (v2026.08.48, PR #16; `navigation-review.md` § C, the third of Tier 3): one of the three things in the masthead was a heading, a sentence and an empty box over 10,108 parties, none of them named. The fifty busiest are now named with what the record holds for each (Union Pacific: 3,345 filings across 318 proceedings), under an A–Z over all of them, a page per initial. The cost was the naming: `Components.display_name` runs four queries per party and took six seconds for the corpus, so `resolve.display_names` applies the same precedence over one ordered read — ten milliseconds — with an agreement test between the two, because drift would print two names for one party. Counting them exposed a second defect: `/stats` counted `party` ROWS where this page counts components, so two masthead pages published different numbers under one sentence; a party is the entity (ADR 0015), so `/stats` counts components now and both say 10,108 | 2026-09-01 |
| Every docket, by its number (v2026.08.49, PR #17; `navigation-review.md` § C, the last of Tier 3): `/coverage` said the record holds 32,605 dockets and offered no way to look at one, and each prefix explainer quoted "the registry holds N X dockets" and stopped. `/dockets` lists every prefix, then every number under it, with what the record holds against each; a proceeding holding nothing is listed all the same. **By number, not by year, which is what the review asked for**: a docket row carries a caption and no date, and 16,805 of 21,807 families hold no entry to infer one from, so an index by year would have omitted three quarters of the registry or invented a date. Large prefixes split into bands of a thousand — permanent addresses where a page number would shift under the next backfill. Review found seven, including the page counting families where `/coverage` counts every docket — 21,807 against 32,605, the same two-numbers-one-sentence defect as `/stats` vs `/parties` earlier the same day | 2026-09-01 |
| Five from the deferred pool (v2026.08.50, PR #18): a `captures` outage longer than the poll's trailing seven-day window leaves days nothing ever re-asks, and `covered()` was letting the watch claim them - it now falls through to the ledger, and an OPEN gap gets no catch-up credit at all. The caption query gained a positive control: every ask reads the no-results envelope as benign, so a criteria rename would answer it for every ask for ever, and a pass that wants any caption now also asks about one sub-docket whose caption it holds and whose number names exactly one row. `reconcile_empty_month` stopped reading the slice-key grammar its own way; `isinstance(True, int)` stopped counting a flag as an ingested row; and 2,233 inline SVGs became two `<symbol>`s and 2,233 `<use>`s, taking FD 36873 down 20% in bytes and elements. Eight review findings fixed, including a control that would have false-alarmed every pass on a family root and one gated on the very thing that goes to zero when the trap sets in | 2026-09-01 |
| A series carries an index, and JSON shape 2 (v2026.08.51, PR #19; navigation-review A7, the operator's answer 2026-09-01): `/d/AB-167` assembled all 2,628 entries of its 995 sub-dockets with a payload and an attachment query each and rendered none, so `is_index` is now decided before they are built - the page falls 0.06 s to 0.02 s and its counts come from folded COUNT(DISTINCT) queries. Shape 2: a JSON address covers what the page at that address covers, where it used to answer with the family whatever was asked, so `/d/AB-167.json` falls 709,798 to 196,209 bytes and `/d/AB-55/sub/794X.json` stops publishing 766 proceedings beside a page showing one line. Also `docs/citator-schema.md`, ADR 0017's six open items drafted into proposals - the schema-critic found 24 defects in the first draft including its headline measurement, and the corrected figure is 98.2% precision after decision 5 for the shipping method, not the 100% first published | 2026-09-01 |
| Schema 17 in production, and the defect that took the site down (v2026.09.1): the first deploy behind ADR 0020's maintenance wall, and the first for which both halves were rehearsed against the real store — `litestream restore` reproduced production exactly (schema and eight table counts, `integrity_check` ok, zero FK violations) and migrations 0014–0017 applied to that copy in 2.2 s. The wall was up **2 m 50 s** and **no coverage gap was recorded, because the record was kept throughout**: the poller captured 16 s before the store was verified at schema 17. What shipped: the citator's five assertion families and reviewer identity (ADRs 0017/0018 — nothing has run a real load yet); `sheet.one_entry`, for a record page that built its entire docket sheet to read one field off it — FD 35087 holds 12,031 of the record's 34,255 comments, so its comment pages cost 21.5 s and our own sitemap offered a crawler all 12,031, which took production down three times on 2026-09-02, once for 6 h 52 m (coverage gap 1, no records missed); measured after the deploy at **0.123 s** from outside, and the Caddy 503 containment removed with it. Also `_wire_url`, which fetched the last three attachments of the comment drain — their URLs carry a raw en dash and had never reached the wire — closing it at 121 unfetched, all genuine refusals resting their 7 days. And ADR 0019's `/metrics`, now scraped by Alloy into Grafana Cloud as `docket_yard_*` beside the host's own vital signs | 2026-09-02 |
| The resize, and Migration A's first load: `docketyard-prod` (small_3_0) became `docketyard-prod-large` (large_3_0: 8 GB, 160 GB) by snapshot, 55 minutes recorded as coverage gap 2, no records missed, the certificate carried inside `caddy-data`, the procedure as run in `infra/deploy/README.md`. Then, under readers: `extract_text.py` v2 on RMI-AI-MACHINE (5,975 stubs, 6 s), `text paginate` (77,567 documents, 19 s over two runs), `text load` (1,104,935 pages and 1,472 MB of payload blobs, 21 minutes over seven resumes — Litestream's TRUNCATE checkpoint and the loader trade the write lock and the pass aborts by design at 30 s, `deferred.md`) and `search rebuild-pages` (build 1, 8 m 49 s under the write lock; index and table both 1,104,935). The store went 346 MB to 3.7 GB; 59,210 documents read from their text layer, 15,086 image-only now hold empty pages awaiting OCR, 3,271 are not PDFs. `ocr-migration.md` items 16–20 landed with it: `SQLITE_TMPDIR` for the dump, `OnFailure=` gauges through Alloy's textfile collector, the 30 s healthcheck | 2026-09-04 |
| Text pages are noindex, and the display omits contact details (v2026.09.3, PR #23; migration 0020, schema 20; ADR 0021 addendum): the operator's two decisions the same day, after the arguments both ways were put. `document_text_display` returns `dy_display_text(text)` — email addresses and separated North American telephone numbers replaced by a marker, by one SQL function the view, the page index and its deletes share, registered on every connection that reads the view; the stored reading is untouched and now immutable by trigger. A bare separated 3-3-4 needs a telephone word beside it, postal addresses are left, and the page says "where a pattern finds them". A pattern change is a new migration; `web` refuses an index built under an older view. Measured before the rebuild: **102,963 of 1,104,935 pages (9.3%) in 34,432 documents change**; a county commission's letterhead reads "Phone [phone omitted] Fax [phone omitted]" with its street address intact. Deployed behind the wall in 29 minutes, of which the page-index rebuild took 27 m 26 s — three times the earlier figure, the function running once per row plus twelve minutes of a competing measurement; a filing's text page with 438 markers and no raw address verified from outside. Schema-critic and two review passes, nine new tests | 2026-09-04 |
| The page search path (v2026.09.4, PR #24; migration 0021, schema 21; ADR 0021 D7 closed): pages of documents reach `/search` and the MCP search by their own FTS5 query path over `page_fts` (`ORDER BY rank LIMIT`, clamped at twenty on every surface, truncation reported), never joined to `search()`, never `/suggest`. A page hit carries who read the page, the band's operand or why there is none, and the scan; the sentences are `store.pages.label` and `band`, now the text page's own too, so the two surfaces cannot drift. The record carrying a document is the earliest-filed, parent before sub-docket, found by the two new indexes on the attachment tables. Measured on the production index: a rare phrase 8 ms, `railroad` 0.43 s over 277,233 pages, `the` 1.3 s over 650,117; from outside after the deploy, `tazewell county` in 0.19 s and `railroad` in 0.66 s. The four published pages that said documents are not searched now say how they are. Schema-critic on the migration, a medium and a low review pass, nine new tests | 2026-09-04 |
| The record page's rail (v2026.09.6; ADR 0013 addendum, accepted 2026-09-04): when `/view` retired into the record page on 2026-09-03 the frame came across and the viewer's rail did not, and nothing replaced it — so a filing or decision page had stopped showing the files it holds, the parties it was filed for, the records either side of it on the sheet, and its citation. All four were only ever on the rail; the operator found it by reading a decision. The rail returns beside the frame in the `.viewer` grid the text page already uses, so no CSS shipped: parties resolved from the "Filed For" cell and linked to `/p/<id>` beside the cell quoted as the Board printed it (the quote is the record, the links are derived from it), files with the hash that is each one's identity and the Board's own copy, neighbours in sheet order, the citation with its copy button, the follow form. `sheet.entry_and_neighbours` was restored rather than rebuilt — the ordering pass that never assembles an entry it will discard. Measured on a production copy, FD 35087 (12,031 comments, the worst docket in the record): `docket_sheet` 235.3 ms, `one_entry` 8.5 ms, this 22.6 ms; 1.09 vs 1.28 ms over 40 ordinary records, which is what a crawler meets. From outside after the deploy that record page answers in **0.13 s** against its own sheet's 1.98 s and 15.6 MB, and its "next" is a comment addressed under its docket — the case that answered 500 in v2026.08.44. Review found one defect, restored with the rail: the follow block named the sub-docket while `/subscribe` folds a follow to the family, so a reader on FD 36873 (Sub-No. 1) would have been told one thing and subscribed to another (navigation-review A6, third time); fixed the sheet's way, heading and scope sentence both, verified in production. Deployed under readers, no migration | 2026-09-04 |
| The two write-lock findings of Migration A (v2026.09.7): both were dated 2026-09-04 in `deferred.md` and both were owed before Migration B's OCR loads, which are larger than the load that found them. **A bulk pass waits the lock out**: `text load` had aborted six times against Litestream's `checkpoint: mode=TRUNCATE err=database is locked` and `text paginate` once, and a shell loop of twelve restarts finished the pass by hand — the rollback is what hands the lock over, so the batch is now rolled back, waited on and REPLAYED, five times doubling from 2 s (`batches.under_lock`, one definition for both callers). Replay is safe because the rollback leaves nothing and the payload blobs are content-addressed, the same property that made a bare abort survivable; a lock that never clears still aborts, and a disk error aborts at once rather than spending a minute of waits on what will not clear. **`search rebuild-pages` is batched**: FTS5's `'rebuild'` reads the content view inside one transaction, which with migration 0020's masking function on every row held the write lock 8 m 49 s, then 27 m 26 s, and cost the poller its 01:03 pass — now `'delete-all'` plus keyset-paged batches of 2,000, with the per-row Python running in the SELECT outside any transaction, which is where the time went. Measured on a synthetic 100,000-page store with a second writer probing every 10 ms: refused the lock on 8 of 21 probes before against 0 of 255 after, for 3.2 s of wall time against 4.0 s; **the production figure at 1.1M rows is deliberately not claimed** and the next real rebuild confirms it (the operator's call, 2026-09-04: a forced rebuild of a correct index was not worth a maintenance window). Taking the rebuild apart cost its atomicity, and the high review pass found five, each verified: every write in it including the mark is under the same retry (the mark is the first lock a rebuild takes, so a contended store failed before one row moved, leaving the index EMPTY and `web` refusing to start); `page_built` reads `rebuilding` from before the first row until after the last, so `web` refuses to start against it, `/search` and the MCP tool say the text index is being rebuilt rather than answering out of a fraction of the record with a 200, and `text load` refuses to run beside a rebuild — both writing `page_fts` would index one rowid twice, which external-content FTS5 takes in silence and a later `'delete'` half-clears. The reverse (a rebuild started under a running load) is not detected and stays in `deferred.md`; the rebuild reports that the signature moved under it. Deployed under readers, no migration; ten new tests, 601 in all | 2026-09-04 |
| No one document takes the page section (v2026.09.8): the two page-search findings dated 2026-09-04 in `deferred.md`. Rank alone put a 300-page assessment's pages 1–20 in the twenty and buried every other document that matched, with nothing on the page to say anything was missing — the record path cannot fail this way because its grain is one row per docket, and this one's is one row per PAGE. Two hundred ranked rows are scanned and folded to three a document (`PAGE_PER_DOCUMENT`, `PAGE_OVERFETCH`) before the cut to twenty, and `/search` and the MCP answer both say when pages were folded away. Verified from outside after the deploy: `railroad` returns twenty pages across **fourteen documents**, three at most from any one. And `PageResults.dropped`, computed and read by nobody, became `docket_yard_page_index_stale_rows_total` on `/metrics` — drift only, and what searches have MET, because a store query for it would be the masking function over 1.1M rows on every scrape; it read 0 on the live store after the deploy, which is the incremental index saying it is in step. Review (medium, three findings, then a low pass) is most of the shape: the fold READS NO TEXT, because SQLite evaluates a view's expression only for the columns selected, so two hundred rows fold on their document alone and the text is fetched for the twenty that survive — measured on a 40,000-page store at 3,827 characters a page, a real page's length, with a term matching every page: **36.7 ms before, 49.0 ms read the naive way, 37.6 ms as shipped**. The first figure published for this said 4 ms and was measured on 800-character pages where masking is too cheap to see; `docs/search.md` carries the corrected numbers and says the old one was wrong. Also from review: the metric counts drift only (a comment's attachment has text and no text address, and is dropped from every search of a healthy store), and the MCP truncation line quoted twenty while fewer were shown and offered "more pages than the 0 shown" when every row was dropped. Deployed under readers, no migration; six new tests, 605 in all | 2026-09-04 |
| The citator sees the record, and the finder ships (v2026.09.9): `citator find` — the half the CLI called missing, for a reason that expired when Migration A loaded 161,801 non-empty pages of decision text. The walk is a store read now, writes findings in `load`'s shape and asserts nothing, one directory per reading channel because a batch carries one measurement (ADR 0018 D8) — a grain the high review pass corrected, since an earlier draft collapsed a mixed-channel document to its majority and produced output `load` refuses. **The whole chain then ran into a `VACUUM INTO` copy, the first time it has run on anything but a fixture**: 20,062 readings, 73,101 findings (41,954 captions, 31,147 citations), 15,164 distinct (citing work, target) edges over 5,294 citing works, **0 failed and 0 out of class**; production's `citation` stays 0 rows, because the load is the operator's. Two things the corpus found that sixty decisions could not: the two ends of ADR 0017 D2's resolution held two spellings of one docket — the Board prints `AB 1182 (Sub-No. 0X)` where the record stores `sub_sequence` NULL — now one renderer with `KEY_VERSION` bumped, and measured to **avoid 39 false edges** rather than gain 43 true ones (a correction to my own first account, which had measured resolution and not the extractor's `kind`); and the citation class can name **31,972 of 32,627 held dockets**, so 655 across 13 prefixes can never be cited to, a recall ceiling nobody had measured. Also: `citator load` stopped claiming there is no review queue — migration 0015 shipped it, `project.py` holds the exposed class out of the projection, and the queues derive from the loaded rows (1,946 / 489 / 1 owed), so the verb was telling an operator the opposite of what the store does. Planning the real load found two blockers that are not steps: no verb declares a measurement, and `reviewer` is 0 rows (`docs/runbook.md` § The citator's first load). Schema-critic on the key change, a high and two low review passes; 627 tests | 2026-09-04 |
| The invariants that were only written down (v2026.09.10): three defects found by checking the store against what the notes and comments claimed, which is how all three had survived. **Migration 0014 said "INGEST MUST KEEP THIS IN STEP" in prose and nothing did** — the citator's work registry held 19,836 rows against 19,839 decisions, three drifted, one for each day since the migration ran. Ingest writes the id now, and **every forward pass reconciles the registry and repairs it, reporting what it repaired**: a bare alarm would have been permanent, because a wave-ingested decision was served years ago and the seven-day window can never re-observe it. Verified on the deploy — capture 110674 healed all three and said so, and the pass-level check then read 0, which is the two layers in the right order. The drift was latent (`Resolution.decision_id` is declared and never assigned, so `cited_decision_id` is NULL and the foreign key cannot fire) and that is exactly why it was the cheap moment. **`compose.yaml` guarded the web tier's key with `:?`, which fails interpolation for the whole file**, so a rotating reader credential stopped `ingest` — nine lines above that file's own capitals forbidding it, and the lesson of coverage gap 1's 6 h 52 m. It is `:-`; the app already refuses to start without its key, and now refuses alone. **A `StoreMismatch` was a `print()`** for the condition that means S3 answered a content-addressed key with bytes that are not that content: `/metrics` carries `docket_yard_document_store_refused_total{kind}` with `mismatch`, `absent` and `unreachable` apart, because the first two send somebody to look and the third means wait — and review caught a 404 landing in the outage bucket. Also `citator declare --scores`, the verb that made a real citator load possible at all: the measurement stamped onto every row comes from the tool that measured it and is re-typed by nobody (the operator's choice of three shapes). The metrics cardinality guard fired on the new labels and was extended rather than loosened. Ingest specialist, schema critic and five review passes; two of my own fixes needed fixing (a lock dropped by waiters, a card printing another measurement's figures) and five claims of mine were corrected against the store. 645 tests | 2026-09-05 |
